# Comment Style

Use comments when they preserve intent that is not obvious from the code.

Good comments explain why a subsystem has a constraint, why a workaround exists, or what must remain stable for generated assets and save data. Avoid comments that restate the next line of code.

For public docs and examples, prefer Mogger Mon terms even when internal engine types still use inherited names.
